generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 1. 使用者 (User)
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  university    String?   // 👈 新增這一行：所屬大學
  accounts      Account[]
  sessions      Session[]
  books         Book[]    // 一個人可以賣很多書
  createdAt     DateTime  @default(now())
  chatRooms     ChatRoom[] @relation("ChatParticipants")
  messagesSent  Message[]  @relation("MessageSender")
  updatedAt     DateTime  @updatedAt
}

// 2. 帳號 (Account) - 用來存 Google 給的登入資料
model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

// 3. 工作階段 (Session) - 判斷使用者有沒有登入
model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// 4. 書籍 (Book)
model Book {
  id            String   @id @default(cuid())
  status        String   @default("ON_SALE")
  isbn          String
  title         String
  authors       String[]
  publisher     String?
  publishedDate String?
  description   String?
  coverUrl      String?
  price         Int
  originalPrice Int?     // 原價 (設為可選，因為有些老書沒定價)
  noteStatus    String?  // 筆記狀況 (例如：無劃記、有筆記)
  condition     String
  courseName    String?
  professor     String?
  tradeMethod   String[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  sellerId      String   // 這本書是誰賣的
  seller        User     @relation(fields: [sellerId], references: [id])
}
// 聊天室 (連結兩個使用者)
model ChatRoom {
  id        String    @id @default(cuid())
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt // 用來排序，有新訊息時會更新這個時間
  
  // 聊天室裡的成員 (買家 & 賣家)
  users     User[]    @relation("ChatParticipants")
  
  // 聊天室裡的所有訊息
  messages  Message[]
}

// 訊息 (每一句話)
model Message {
  id         String   @id @default(cuid())
  content    String   // 訊息內容
  createdAt  DateTime @default(now())
  
  // 誰傳的？
  senderId   String
  sender     User     @relation("MessageSender", fields: [senderId], references: [id])
  
  // 屬於哪個聊天室？
  chatRoomId String
  chatRoom   ChatRoom @relation(fields: [chatRoomId], references: [id], onDelete: Cascade)
}